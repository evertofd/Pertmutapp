<%= form_for([@product, @product.comment.build]) do |f|%>
    <form class="form-inline" role="form">


          <div class="form-group">
            <%= f.label :name %><br>
            <%= f.text_field :name, class: "form-control bg-white border-left-0 border-md"%>
          </div>

          <div class="form-group">
            <%= f.label :body %><br>
            <%= f.text_area :body, class: "form-control bg-white border-left-0 border-md"%>
          </div>
                <%= f.submit "Enviar", class: "btn btn-primary" %>
            </p>
        <% end %>
    </form>
